<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <!--
          Debug-only diagnostics launcher. Present in the debug source set only,
          so it can never ship in a release build.
        -->
        <activity
            android:name="com.ps26104.aawaz.diagnostics.DiagnosticsActivity"
            android:label="Aawaz Diag"
            android:exported="true"
            android:theme="@android:style/Theme.Material.Light">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
